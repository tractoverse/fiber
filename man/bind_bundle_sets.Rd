% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bind.R
\name{bind_bundle_sets}
\alias{bind_bundle_sets}
\title{Combine bundles and/or bundle_sets into a single bundle_set}
\usage{
bind_bundle_sets(..., bundle_data = NULL, set_data = NULL)
}
\arguments{
\item{...}{\link{bundle} objects or \link{bundle_set} objects to combine. Named bare
\link{bundle} arguments will carry their name into the resulting set.}

\item{bundle_data}{A named list of per-bundle vectors to attach to the
resulting \link{bundle_set}. Defaults to the \verb{@bundle_data} of the first
\link{bundle_set} input if present.}

\item{set_data}{A named list of set-level scalar metadata to attach to the
resulting \link{bundle_set}. Defaults to the \code{set_data} of the first
\link{bundle_set} input (if present) or an empty list.}
}
\value{
A \link{bundle_set} containing all input bundles.
}
\description{
Accepts any mix of \link{bundle} objects or \link{bundle_set} objects. All bundles are
collected into a flat list and wrapped in a new \link{bundle_set}. Bare \link{bundle}
arguments may optionally be named; unnamed bundles are included without a
name label.
}
\examples{
sl <- streamline(points = cbind(X = runif(5), Y = runif(5), Z = runif(5)))
b1 <- bundle(streamlines = list(sl))
b2 <- bundle(streamlines = list(sl))

# two named bare bundles
bs <- bind_bundle_sets("sub-01" = b1, "sub-02" = b2)
bs@n_bundles   # 2
bs@bundle_data$if_from_input_list  # c("sub-01", "sub-02")

# combine two bundle_sets
bs1 <- bundle_set(list("sub-01" = b1))
bs2 <- bundle_set(list("sub-02" = b2))
bs_all <- bind_bundle_sets(bs1, bs2)
bs_all@n_bundles  # 2
}
