% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bind.R
\name{bind_bundles}
\alias{bind_bundles}
\title{Combine streamlines and/or bundles into a single bundle}
\usage{
bind_bundles(..., streamline_data = NULL, bundle_data = NULL)
}
\arguments{
\item{...}{One or more \link{streamline} or \link{bundle} objects.}

\item{streamline_data}{A named list of per-streamline vectors to attach to
the resulting \link{bundle}. Defaults to the \verb{@streamline_data} of the first
\link{bundle} input if one is present.}

\item{bundle_data}{A named list of bundle-level scalar metadata to attach to
the resulting \link{bundle}. Defaults to an empty list (or the \code{bundle_data} of
the first \link{bundle} input if one is present and \code{bundle_data} is not
supplied).}
}
\value{
A \link{bundle} containing all input streamlines.
}
\description{
Accepts any mix of \link{streamline} and \link{bundle} objects. All streamlines are
collected into a flat list and wrapped in a new \link{bundle}. \code{bundle_data}
from the first \link{bundle} argument (if any) is preserved; pass your own via
the \code{bundle_data} argument to override. Similarly for \code{streamline_data}.
}
\examples{
sl1 <- streamline(points = cbind(X = runif(5), Y = runif(5), Z = runif(5)))
sl2 <- streamline(points = cbind(X = runif(5), Y = runif(5), Z = runif(5)))
b1 <- bundle(streamlines = list(sl1))
b2 <- bundle(streamlines = list(sl2))

# combine two bundles
b_all <- bind_bundles(b1, b2)
b_all@n_streamlines  # 2

# mix a bundle and a loose streamline
b_mixed <- bind_bundles(b1, sl2)
b_mixed@n_streamlines  # 2
}
